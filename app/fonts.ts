import { Inter, Noto_Sans } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const noto = Noto_Sans({
    subsets: ["latin"],
    display: "swap",
})